/**
 * Where the source lives. The public page says this address four times in four
 * shapes (a button, a link, a footer line, an issue tracker), so it is written
 * down once.
 */

export const REPO_URL = "https://github.com/Mixer159/Workeee";
export const REPO_ISSUES_URL = `${REPO_URL}/issues`;
export const REPO_LICENSE_URL = `${REPO_URL}/blob/main/LICENSE`;

/** The address as a person reads it, without the scheme. */
export const REPO_LABEL = "github.com/Mixer159/Workeee";

export const LICENSE_NAME = "MIT";
