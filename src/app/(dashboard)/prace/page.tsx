import { WorkspaceCanvas } from "@/components/workspace/workspace-canvas";

/** Optional focused mode: conversations on the left, active task in the middle. */
export default function WorkModePage() {
  return <WorkspaceCanvas />;
}
